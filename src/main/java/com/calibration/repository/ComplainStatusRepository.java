package com.calibration.repository;

import com.calibration.model.ComplainStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComplainStatusRepository extends JpaRepository<ComplainStatus, Integer> {
}
